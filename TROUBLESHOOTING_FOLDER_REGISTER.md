# عیب‌یابی خطای ثبت پوشه نقشه (400 Bad Request)

## علل احتمالی خطای 400

### 1. نام نقشه تکراری است
**پیام خطا**: `نام نقشه تکراری است. لطفاً نام دیگری انتخاب کنید`

**راه‌حل**:
- یک نام منحصر به فرد برای نقشه انتخاب کنید
- نام‌های نقشه‌های موجود را در لیست نقشه‌های آفلاین بررسی کنید

---

### 2. فایل SQLite در پوشه یافت نشد
**پیام خطا**: `هیچ فایل SQLite (.sqlitedb) در پوشه '{folder_path}' یافت نشد`

**علت**: پوشه انتخابی فایل‌های `.sqlitedb` ندارد یا ساختار درست نیست.

**بررسی**:
```bash
# بررسی وجود فایل‌های sqlitedb
find /path/to/folder -name "*.sqlitedb"

# یا در Windows PowerShell
Get-ChildItem -Path "D:\maps\sat" -Recurse -Filter "*.sqlitedb"
```

**ساختار صحیح**:
```
sat/
└── z3/
    └── 0/
        └── 0/
            └── 0.0.sqlitedb
```

**راه‌حل**:
- مطمئن شوید پوشه انتخابی شامل فایل‌های `.sqlitedb` است
- ساختار باید به صورت `z{zoom}/{x_group}/{y_group}/{x_chunk}.{y_chunk}.sqlitedb` باشد

---

### 3. پوشه یافت نشد
**پیام خطا**: `پوشه '{folder_value}' یافت نشد`

**علت**: مسیر وارد شده اشتباه است یا پوشه وجود ندارد.

**بررسی**:
1. مسیر نسبی را از ریشه `FILESYSTEM_TILE_ROOT` بررسی کنید
2. در داشبورد → Maps → ثبت پوشه، از لیست پوشه‌های موجود استفاده کنید

**مثال**:
- اگر `FILESYSTEM_TILE_ROOT = /mnt/maps`
- و پوشه شما: `/mnt/maps/sat/z3/0`
- مسیر نسبی وارد شده باید: `sat/z3/0` باشد

---

### 4. مسیر خارج از محدوده مجاز
**پیام خطا**: `مسیر خارج از محدوده مجاز ({base_root}) است`

**علت**: پوشه انتخابی خارج از مسیر ریشه (`FILESYSTEM_TILE_ROOT`) است.

**راه‌حل**:
- مسیر باید داخل `FILESYSTEM_TILE_ROOT` باشد
- اگر از هارد خارجی استفاده می‌کنید، مطمئن شوید که در `docker-compose.yml` به درستی mount شده است

---

### 5. پوشه ریشه یافت نشد
**پیام خطا**: `پوشه ریشه نقشه یافت نشد`

**علت**: متغیر `FILESYSTEM_TILE_ROOT` تنظیم نشده یا مسیر اشتباه است.

**بررسی**:

#### در `.env`:
```env
FILESYSTEM_TILE_ROOT=/mnt/external-maps
# یا
FILESYSTEM_TILE_ROOT=backend/static/maps
```

#### در Docker:
```bash
# بررسی مقدار متغیر در کانتینر
docker exec -it kalk-api-1 env | grep FILESYSTEM_TILE_ROOT

# بررسی وجود پوشه
docker exec -it kalk-api-1 ls -la /mnt/external-maps
```

---

## مراحل عیب‌یابی گام به گام

### مرحله 1: بررسی تنظیمات

1. **بررسی متغیر `FILESYSTEM_TILE_ROOT`**:
   ```bash
   # در backend/.env یا docker-compose.yml
   grep FILESYSTEM_TILE_ROOT .env
   ```

2. **بررسی وجود پوشه ریشه**:
   ```bash
   # Linux/Mac
   ls -la /mnt/external-maps
   
   # Windows (PowerShell)
   Test-Path "D:\maps"
   ```

### مرحله 2: بررسی ساختار پوشه

1. **بررسی وجود فایل‌های SQLite**:
   ```bash
   find /mnt/maps -name "*.sqlitedb" | head -5
   ```

2. **بررسی ساختار**:
   ```bash
   tree -L 4 /mnt/maps/sat
   ```

### مرحله 3: استفاده از داشبورد

1. **بررسی لیست پوشه‌های موجود**:
   - به **Dashboard → Resources → Maps** بروید
   - روی **"ثبت پوشه"** کلیک کنید
   - لیست پوشه‌های موجود باید نمایش داده شود
   - اگر لیست خالی است، احتمالاً `FILESYSTEM_TILE_ROOT` اشتباه است

2. **انتخاب پوشه از لیست**:
   - از dropdown لیست پوشه‌ها، پوشه مورد نظر را انتخاب کنید
   - این کار از خطاهای تایپی جلوگیری می‌کند

### مرحله 4: بررسی لاگ‌ها

```bash
# لاگ‌های API
docker logs kalk-api-1 --tail 50

# یا اگر از docker compose استفاده می‌کنید
docker compose logs api --tail 50
```

---

## مثال‌های کاربردی

### مثال 1: ثبت نقشه با مسیر نسبی

**ساختار فایل‌ها**:
```
/mnt/external-maps/
└── sat/
    └── z3/
        └── 0/
            └── 0/
                └── 0.0.sqlitedb
```

**تنظیمات**:
- `FILESYSTEM_TILE_ROOT = /mnt/external-maps`

**در داشبورد**:
- نام نقشه: `نقشه ماهواره‌ای - Zoom 3`
- مسیر پوشه: `sat/z3/0/0` (مسیر نسبی از ریشه)

---

### مثال 2: استفاده از لیست پوشه‌ها

1. در داشبورد روی **"ثبت پوشه"** کلیک کنید
2. لیست پوشه‌ها را ببینید (اگر خالی است، مشکل از `FILESYSTEM_TILE_ROOT` است)
3. از dropdown یک پوشه انتخاب کنید
4. نام نقشه را وارد کنید
5. روی **"ذخیره"** کلیک کنید

---

## بررسی در کد Frontend

اگر هنوز مشکل دارید، در کنسول مرورگر (F12) بررسی کنید:

```javascript
// خطای دقیق در console.error
console.error('خطای ثبت پوشه:', error);
```

این خطا شامل جزئیات دقیق از backend است.

---

## نکات مهم

1. **مسیر نسبی vs مطلق**: همیشه از مسیر نسبی استفاده کنید که از `FILESYSTEM_TILE_ROOT` محاسبه می‌شود
2. **ساختار پوشه**: ساختار باید دقیقاً `z{zoom}/{x_group}/{y_group}/` باشد
3. **فایل‌های SQLite**: حداقل یک فایل `.sqlitedb` باید در پوشه یا زیرپوشه‌های آن وجود داشته باشد
4. **نام نقشه**: نام باید منحصر به فرد باشد

---

## تماس با پشتیبانی

اگر پس از بررسی تمام موارد بالا مشکل حل نشد:

1. لاگ‌های کامل را جمع‌آوری کنید
2. ساختار پوشه و مسیرهای استفاده شده را ذکر کنید
3. پیام خطای دقیق را کپی کنید

**اطلاعات مورد نیاز**:
- مقدار `FILESYSTEM_TILE_ROOT`
- ساختار پوشه (tree یا ls)
- پیام خطای دقیق از console
- لاگ‌های backend

