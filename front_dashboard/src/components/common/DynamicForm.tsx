import React from 'react';
import { Alert } from '@mui/material';

const DynamicForm: React.FC = () => {
  return <Alert severity="info">DynamicForm حذف شده و دیگر استفاده عملیاتی ندارد.</Alert>;
};

export default DynamicForm;
